如果父对象有overflow：hidden属性，子对象中的position属性是relative或者absolute，那么在ie6和ie7下父对象的overflow会失效，
解决办法是给父对象加relative属性。

动画效果 animate();
事件强制停止 用stop()

css3属性 border-radius:10px
ie使用CSS3属性 behavior: url(ie-css3.htc)